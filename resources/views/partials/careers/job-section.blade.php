@if($content)
    <section class="careers-job-section">
        <h2 class="careers-job-section-title">{{ $title }}</h2>
        <div class="careers-prose">{!! $content !!}</div>
    </section>
@endif
